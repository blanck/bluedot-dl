[project]
name = "bluedot-dl"
version = "0.1.0"
description = "Download videos from BlueDot HQ collections"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["httpx", "rich"]

[project.scripts]
bluedot-dl = "main:main"
